name = "dying-notes-api"
main = "src/index.js"
compatibility_date = "2026-09-01"
preview_urls = false

# The Worker only answers the PWA's origin (plus local dev ports).
# Set the real secret with: npx wrangler secret put COHERE_KEY
[vars]
ALLOWED_ORIGINS = "https://mustafaelaalem.github.io,http://localhost:4173,http://localhost:4174,http://localhost:4175,http://localhost:4176,http://localhost:5173"
